Run TypeScript type checking across all packages.

1. Run `bun turbo typecheck`
2. If there are errors, group them by package/application
3. For each error, show the file path, line number, and error message
4. Suggest fixes for the most common errors
