Run the teardown script to remove configured services.

1. Run `bun scripts/teardown.ts` interactively
2. Follow the prompts — each step requires explicit confirmation
3. Report what was removed and what was skipped
