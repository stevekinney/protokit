# INTEROP-001: known-failure baseline for the pinned
# `@modelcontextprotocol/conformance` CLI's `2025-11-25` (legacy) server run.
#
# `--expected-failures` is a feature of the pinned conformance package
# itself (not something this repository invented): it turns a listed
# scenario's failure into an informational "expected failure" line instead
# of a hard CI failure, AND fails the run if a listed scenario starts
# passing (a "stale baseline entry") -- so this file cannot silently rot
# into hiding a real regression once one of these is genuinely fixed.
#
# Every scenario listed here was verified directly against a real,
# unmodified instance of this server (`packages/mcp/src/conformance-server.ts`)
# running locally, not assumed from the scenario name:
#
# - `tools-call-sampling`, `tools-call-elicitation`,
#   `elicitation-sep1034-defaults`, `elicitation-sep1330-enums`: each times
#   out (`MCP error -32001: Request timed out`) rather than completing.
#   `packages/mcp/src/conformance-fixture-registration.ts`'s `test_sampling`
#   / `test_elicitation*` fixtures correctly call the SDK's own
#   `sendRequest` (a real server-initiated `sampling/createMessage` /
#   `elicitation/create` request) and correctly catch a *rejection* with a
#   graceful `isError: true` result -- but nothing bounds how long that
#   `sendRequest` await can run, and this repository's own conformance
#   suite (`conformance-modern.test.ts` / `conformance-legacy.test.ts`)
#   never exercises sampling or elicitation at all, so there is no existing
#   evidence either way for whether this is (a) the external CLI's built-in
#   test client simply not implementing an automatic sampling/elicitation
#   responder for `server` conformance runs, or (b) a genuine unbounded-await
#   gap on this server's side that a real client's slow-to-respond human-in
#   -the-loop elicitation would also trigger. Root-causing that distinction
#   needs deeper SDK-internals investigation than this item's scope covers --
#   flagged explicitly rather than guessed at or silently patched.
server:
  - tools-call-sampling
  - tools-call-elicitation
  - elicitation-sep1034-defaults
  - elicitation-sep1330-enums
